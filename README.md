# ZiBY Apps Portfolio

Цей проект є веб-сайтом портфоліо для мобільних додатків ZiBY. Він створений за допомогою React, Vite та TypeScript і налаштований для автоматичного розгортання на GitHub Pages.

## 🛠 Налаштування та запуск локально

1. **Встановлення залежностей**:

   ```bash
   npm install
   ```

2. **Запуск локального сервера розробки**:
   ```bash
   npm run dev
   ```
   Сайт буде доступний за адресою, яку покаже термінал (зазвичай `http://localhost:5173/`).

## 🚀 Як оновити сайт (Деплой)

Щоб оновити версію сайту, яка доступна в інтернеті (на GitHub Pages), виконайте одну команду в терміналі:

```bash
npm run deploy
```

Ця команда автоматично:

1. Збере оптимізовану версію сайту (`npm run build`).
2. Завантажить файли у гілку `gh-pages` на GitHub.
3. Опублікує зміни.

_Примітка: Після успішного виконання команди може знадобитися кілька хвилин (1-5 хв), щоб зміни з'явилися на сайті._

## 📝 Як додати новий додаток

1. **Підготуйте логотип**:
   - Збережіть зображення логотипу в папку `public/logos/`.

2. **Оновіть дані**:
   - Відкрийте файл `src/data/apps.ts`.
   - Додайте новий об'єкт у масив `apps`. Приклад:
     ```typescript
     {
       id: 'MyNewApp', // Використовується в URL політики конфіденційності (без пробілів)
       appleId: '1234567890', // ID в App Store (опціонально)
       googleId: 'com.example.app', // ID в Google Play (опціонально)
       name: 'My New App', // Назва додатку
       description: 'Опис вашого чудового додатку.',
       logo: '/logos/my-new-app.png', // Шлях до логотипу
       withAdmob: true, // true, якщо є реклама (для політики конфіденційності)
     },
     ```

3. **Запустіть деплой**:
   - Виконайте `npm run deploy`, щоб зміни з'явилися в інтернеті.
